function getComputerChoice (){
const choice = ["Rock", "Paper", "Scissor"]
const random = math.floor(math.random()*choice.length)
console.log(random)
}

// create an array with rock paper scissors //  DONE
// make the computer select one of the three at random// var randomNumber = Math.floor(Math.random()*textArray.length);
// print randomly selected thing //